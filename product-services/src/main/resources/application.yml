server:
  port: 8084
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/productdb
    username: springuser
    password: springpass
  jpa:
    hibernate:
      ddl-auto:update
  application:
    name: product-services
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
